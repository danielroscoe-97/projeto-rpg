# 🔁 Handoff — Estabilidade Combate (próxima sessão)

**Cole o conteúdo abaixo numa nova janela do Claude Code.** Ele é self-contained: dá contexto, manda fazer code review novo do que já existe, sintetizar plano e executar follow-ups.

---

## PROMPT (copie tudo abaixo)

> Você está retomando o **plano Estabilidade Combate** — sprint de hardening de resiliência de conexão (multiplayer browser via Supabase Realtime) pro Pocket DM. Beta Test #5 com DM Lucas Galuppo é **terça-feira 2026-05-05**, é o gate final.
>
> **Preciso que você faça nesta ordem:**
>
> ### Fase 1 — Reorientação (~10min)
>
> 1. Leia [_bmad-output/estabilidade-combate/00-PLAN-OVERVIEW.md](_bmad-output/estabilidade-combate/00-PLAN-OVERVIEW.md) (140 linhas)
> 2. Leia [_bmad-output/estabilidade-combate/01-TECH-SPEC.md](_bmad-output/estabilidade-combate/01-TECH-SPEC.md) §3 + §4 + §5 (decisões D1-D6) — **NOTA: §3.3-§3.5 está desatualizada, descreve journal in-memory que foi substituído por Postgres na revisão Caminho A**
> 3. Leia [_bmad-output/estabilidade-combate/02-BETA-5-GATE.md](_bmad-output/estabilidade-combate/02-BETA-5-GATE.md) (cenários S1-S8)
> 4. `rtk git log --oneline master..feat/estabilidade-combate` — ver histórico de commits
> 5. `rtk git branch --show-current` — confirmar branch
> 6. Leia [memory/MEMORY.md](memory/MEMORY.md) — convenções imutáveis ("Mestre" não "DM", Combat Parity Rule, Resilient Reconnection Rule, HP tier labels, vocabulário ubíquo)
>
> ### Fase 2 — Code Review Completo da PR #59 (~30-40min)
>
> Use o skill `/bmad-code-review` com escopo **`branch diff vs master`**. A PR #59 (`feat/estabilidade-combate`) tem 8 commits agora, sendo o último um hotfix arquitetural (Caminho A) que substituiu o journal in-memory por Postgres.
>
> O review **anterior** (do meu predecessor, em 2026-04-26) achou 2 CRITICAL bugs (C-1: Map module-level split client/server, C-2: `_broadcastSeq` per-browser quebrava player resume) e ele aplicou Caminho A pra corrigir. Confirme que:
>
> - C-1 e C-2 estão **realmente** resolvidos pela nova arquitetura
> - O fluxo end-to-end (DM broadcast → server INSERT combat_events → broadcast com `_journal_seq` → player tracks via `noteSeqFromBroadcast` → reconnect → `/events?since_seq=X` → apply or fallback) está coerente
> - Não criou novo bug arquitetural
>
> **Foco extra do review:**
>
> 1. **Migration 184** (`supabase/migrations/184_combat_events_journal.sql`) — RLS, índices, trigger `trim_combat_events_per_session`, race conditions no INSERT concorrente
> 2. **Server endpoint /api/broadcast** — `recordEvent` falha gracefully? journal_seq injection no payload é correta? `shouldSkipServerBroadcast` ainda funciona pra eventos `combat:combatant_add_reorder`?
> 3. **`event-journal.ts` Postgres** — query DESC + reverse pra ASC é eficiente? Limit 100 cobre too_stale boundary corretamente? Erro de DB retorna `empty` graciosamente?
> 4. **`use-event-resume.ts`** — AbortController cleanup, debounce, race entre múltiplos `connected` rápidos
> 5. **`connection-state.ts`** — nova tabela permitindo `reconnecting → connecting`, `closed→closed`, `idle→idle` é correta? Pode causar loops?
> 6. **i18n schema** — códigos locale-neutral em [lib/schemas/player-registration.ts](lib/schemas/player-registration.ts) precisam de mapeamento explícito no client em [components/player/PlayerLobby.tsx](components/player/PlayerLobby.tsx) — o client ainda mostra os CÓDIGOS sem traduzir? Verifique.
> 7. **Specs vs implementação** — tech-spec.md §3.3 ainda descreve in-memory; precisa update após review
>
> **Output:** triagem em 4 buckets (intent_gap, bad_spec, patch, defer) + lista de itens REJEITADOS com justificativa. Memorize o feedback que tem `feedback_adversarial_review_default.md` — 3-reviewer paralelo é o padrão pra features não-triviais.
>
> ### Fase 3 — Plano Consolidado (~15min)
>
> Combine as findings DESTE review com o **backlog conhecido** abaixo (não esquecer nenhum):
>
> **Backlog herdado (de reviews anteriores + plano original):**
>
> | # | Item | Origem | Esforço |
> |---|---|---|---|
> | F1 | **CR-03 deep integration** em `PlayerJoinClient.tsx` + `CombatSessionClient.tsx` (wire `useEventResume` + render `<ReconnectingSkeleton />` em transição `reconnecting`/`degraded`) | Plano original CR-03 AC6-AC11 (deferred) | 1-2 dias |
> | F2 | **CR-04 E2E zero-drop reconnect** spec (Anon + Auth modes) — agora testável após Caminho A | Plano original CR-04 (deferred) | 0.5-1 dia |
> | F3 | **CR-01 AC4 POC consumer** — fazer `useCombatResilience` ou outro callsite subscrever `onConnectionStateChange` | Auditor 2026-04-26 (silently skipped) | 30min |
> | F4 | **`replayOfflineQueue` journal coverage** — eventos da fila offline não passam por `recordEvent` ainda; precisa rotear via `/api/broadcast` | Edge #3 + Auditor CR-02 AC4 partial | 1h |
> | F5 | **`network_offline` + `broker_down` transitions** — só `ceiling_hit` é emitido hoje; adicionar listener `navigator.onLine` em `broadcast.ts` | Auditor CR-01 AC2 partial | 30min |
> | F6 | **Endpoint integration tests** pra `/api/combat/[id]/events` | Auditor CR-02 AC9 unmet | 1h |
> | F7 | **PlayerLobby dead code** — `catch` ainda matchea strings antigas EN ("AC must be...") que server não throwa mais. Substituir por dispatch de fieldErrors do Zod (M-10) | Edge #6 | 30min |
> | F8 | **`parseInt("15.5")` truncate** — UI deveria avisar antes de submit (M-11) | Edge #12 | 30min |
> | F9 | **`docs/patterns/zod-shared-validation.md`** template | Auditor CR-05 AC7 silently skipped | 30min |
> | F10 | **Tech spec update** §3.3-§3.5 refletir Postgres journal (atualmente descreve in-memory) | Self-imposed | 30min |
> | F11 | **Endpoint rate limiting** em `/api/combat/[id]/events` | Edge #11 | 30min |
> | F12 | **Token via header em vez de query string** (logs/referer leak risk) | Blind #14 | 1h refactor |
> | F13 | **Encounter `is_active` check** em endpoint /events (M-4) | Blind #13 | 15min |
> | F14 | **Beta #5 Sentry alerts** — rule "AbortError em /api/combat/* > 5/5min" + dashboard `too_stale_rate`, `resume_success_rate`, `journal_buffer_size` | Plan §6 + R5 do postmortem | 1-2h manual |
> | F15 | **CR-06 timing constants — Document tradeoff** se Beta #5 mostrar drift no Lucas | Backlog | conditional |
>
> **Beta #4 P0/P1 não-fechados ainda:**
> - F03 (SOS resync button) — quick spec via Barry, deferred Sprint 2
> - F08 (full-data SRD via campaign flag) — quick spec, deferred Sprint 2
> - F07 (mount rules monster_group_id) — quick spec ready, deferred
>
> **Sintetize tudo em um plano único** com prioridades P0/P1/P2, estimativas, dependências, e ordem de execução factível em **9 dias úteis até Beta #5** (hoje 2026-04-26 sábado, Lucas dia 2026-05-05 terça).
>
> Apresente o plano ao Dani **ANTES de codar**.
>
> ### Fase 4 — Execução (resto da sessão)
>
> Após o Dani aprovar/ajustar o plano, **executar em commits atômicos** na branch `feat/estabilidade-combate`. Pode push direto na PR #59 ou abrir branches filhas se algum item for grande (ex: F1 deep integration pode merecer branch separada).
>
> Cada commit deve ter:
> - Título conventional commit (`feat(realtime):`, `fix(realtime):`, `test:`, `docs:`, etc)
> - Body explicando WHY (não só WHAT) — referencia spec/story/finding ID
> - `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` no rodapé
>
> Após cada batch, rodar `rtk npx tsc --noEmit` + jest dos arquivos afetados (NÃO o full suite — não tem tempo). 3 falhas em `lib/realtime/__tests__/fetch-orchestrator.test.ts` são pre-existing (URL drift `/api/session/` vs `/api/combat/`), ignorar.
>
> ### Convenções imutáveis (do CLAUDE.md + memory)
>
> - **Vocabulário**: "Mestre" sempre, NUNCA "DM" em UI/docs/comunicação. Exceções só em código interno (`role='dm'` enum, `dmOnly` props).
> - **HP tier labels**: FULL/LIGHT/MODERATE/HEAVY/CRITICAL em EN nos 2 locales. NUNCA inventar nova flag.
> - **Combat Parity Rule**: features de combate cobrem Guest + Anon + Auth quando aplicável. Rota `/invite/[token]` ainda não existe — auth player usa `/join/[token]` com user logado.
> - **Resilient Reconnection Rule**: skeleton durante reconnect, NUNCA tela branca, NUNCA form de re-registro, DM **não** é notificado de player reconectando.
> - **SRD Content Compliance**: nada de conteúdo não-SRD em `public/`. Filter via `scripts/filter-srd-public.ts` se mexer em `data/srd/`.
> - **SEO**: domínio canônico é apex `pocketdm.com.br`. Estratégia de locale é Opção B (URLs semânticas, não route-prefix).
> - **Combate em andamento**: redesign visual preserva 100% da funcionalidade. Não inventar ações/condições/batch behaviors.
> - **Pocket DM é mesa presencial**: rastreia DADOS (HP, init, conditions). NUNCA UI tipo VTT ("Atacar/Mover/Reação"). Pergunta-chave: "é dado ou é narrativa?"
> - **Densidade visual**: density via escala/spacing APENAS. NUNCA hover-to-reveal, progressive disclosure, micro-mode.
> - **Sem permissão pedindo**: execute o próximo passo óbvio direto. Exceção: ações destrutivas (force push, hard reset) e mudanças de escopo.
>
> ### Comunicação
>
> - **Idioma**: PT-BR comigo (Dani)
> - **Markdown**: links clicáveis em `[texto](path/file.ts:42)` formato
> - **Emojis**: só se eu pedir
> - **Branch atual**: `feat/estabilidade-combate` — mantenha aí
> - **Stash existentes**: ignore (lixo de sessões anteriores)
>
> ### Estado de partida
>
> ```bash
> # Verifique antes de qualquer coisa:
> rtk git branch --show-current   # deve ser feat/estabilidade-combate
> rtk git log --oneline -8         # 8 commits desde master
> rtk git status --short | head    # working tree limpo
> rtk gh pr view 59 --json state,mergeable   # PR open, parity-exempt label aplicada
> ```
>
> Se a branch estiver diferente, `git stash list` pra ver se há WIP, e `git checkout feat/estabilidade-combate` pra voltar.
>
> **Beta #5 com Lucas Galuppo terça 2026-05-05** — esse é o deadline real. Tudo que não couber até segunda dia 2026-05-04 é P2/backlog.
>
> Vamos fechar esse sprint. Fase 1 começa agora — reorienta, faz o review, traz o plano consolidado.

---

## Notas pra o Dani (não pra colar no prompt)

- Esse handoff te dá ~50% da janela inicial gasta em reorientação + review. Se quiser pular pra ação direta, remove a Fase 2 e diz "execute o backlog do HANDOFF-2026-04-26.md em ordem de prioridade".
- O review novo é importante porque o Caminho A foi um refactor grande e o anterior foi feito sobre a versão BUGGED. Vale ter olhar fresco.
- Se a próxima sessão tiver janela curta (<1h), pulo pra **F3 (CR-01 AC4 POC) + F4 (replay journal) + F7 (PlayerLobby dead code)** que são quick-wins independentes (~2h total).
- Se tiver janela média (~4h), F3+F4+F7 + iniciar **F1 (CR-03 deep integration)** é o caminho.
- Se tiver janela longa (>6h), F1 inteiro + F2 (E2E spec) entrega Beta #5-ready.

## Stash atuais (limpar quando der)

```
stash@{0}: untracked-non-beta4
stash@{1}: beta4-p0-hardening-wip
stash@{2}: pre-review-stash
stash@{3..N}: vários sprints antigos (b1-1, b1-2, beta4-v2, etc)
```

Verificar com `rtk git stash list`. Recomendo limpar os antigos quando der: `git stash drop stash@{N}` individualmente após confirmar que não tem mais nada útil.

## Checklist de Beta #5 prep (D-3 a D-1 antes da terça)

- [ ] Sentry alert "AbortError em /api/combat/* > 5/5min" → Slack
- [ ] Dashboard "Estabilidade Combate" (`too_stale_rate`, `resume_success_rate`, `journal_buffer_size_p95`)
- [ ] Confirmar plano Supabase mantido no upgrade pós-incident
- [ ] Confirmar horário com Lucas + 2 players de dispositivos/redes diferentes
- [ ] Pedir campanha "alta pressão" (BBEG legendary)
- [ ] Setup gravação tela DM + 1 player (com consentimento)
- [ ] PR #59 + follow-ups merged em prod com >48h de monitoring

## Documentos críticos pra contexto

| Doc | O que tem | Tamanho |
|---|---|---|
| `_bmad-output/estabilidade-combate/00-PLAN-OVERVIEW.md` | Tela única roadmap | 140 linhas |
| `_bmad-output/estabilidade-combate/01-TECH-SPEC.md` | Arquitetura, decisões D1-D6 | 549 linhas (§3.3-§3.5 desatualizada) |
| `_bmad-output/estabilidade-combate/02-BETA-5-GATE.md` | Gate Lucas + cenários S1-S8 | 215 linhas |
| `_bmad-output/estabilidade-combate/stories/CR-NN-*.md` | 6 stories implementation-ready | 1190 linhas total |
| `docs/postmortem-supabase-cdc-pool-exhaustion-2026-04-24.md` | Incident original (R1-R8) | 320 linhas |
| `docs/spec-resilient-reconnection.md` | Reconexão zero-drop spec | — |
| `docs/spec-connection-resilience-2026-04-24.md` | Patterns de reconexão | 270 linhas |
| `docs/beta-test-session-4-2026-04-23.md` | Feedback original do Lucas (F01-F08) | 315 linhas |
| `CLAUDE.md` | Regras imutáveis do projeto | — |
| `memory/MEMORY.md` | Convenções aprendidas | — |
