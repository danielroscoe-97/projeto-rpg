# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Monster a Day source files — keep locally, never delete
/monster a day - nao apagar/

# Raw images for blog treatment — downloaded assets, not tracked
/raw-images/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# temp workspace for build baselines, parity manifests, scratch files
/.tmp/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*.local
.env
.env.e2e

# Google service account keys (GSC export) — never commit the raw JSON key
*.gsa.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local reference assets (not part of the codebase)
"referencia visual"/
referencia visual/
story-4-2-diff.txt
supabase/.temp/

.vercel
scripts/orchestrator/.env

# BMAD Orchestrator
scripts/orchestrator/.env
scripts/orchestrator/.watcher-state.json
scripts/orchestrator/.queue-state.json
scripts/orchestrator/logs/
scripts/orchestrator/node_modules/
.playwright-mcp/
scripts/audios/
scripts/audiosv2/
e2e/results/
playwright-report/
ms-playwright/

# QA evidence screenshots (local only — never commit/deploy)
qa-evidence/

# Personal campaign files — not part of the app
public/fichas/

# Large video files — use CDN/storage, not public/
public/video/*.mp4

# Internal docs served via admin route, not public
docs/internal/
C\:\\Users\\*/

# Large sound effect source files
efeitos sonoros/
scripts/.mad-cache/
__pycache__/
*.pyc
scripts/monsters-mad-reddit.json
